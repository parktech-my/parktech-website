---
import { Icon } from "astro-icon";
import Link from "@components/ui/link.astro";

const { product } = Astro.props;
---

<div>
  <div
    class="flex flex-col w-full order-first lg:order-none border-2 border-[#D8DEE9] border-opacity-50 py-5 px-6 rounded-md">
    <div class="text-center">
      <h4 class="text-lg font-bold text-blue-600">
        {product.name}
      </h4>
      <!-- <p
        class="mt-3 text-4xl font-bold text-black md:text-4xl">
        {
          product.price && typeof product.price === "object"
            ? product.price.monthly
            : product.price
        }
      </p> -->
      <!-- {
        product.price.original && (
          <p class="mt-1 text-xl font-medium text-gray-400 line-through md:text-2xl">
            {product.price.original}
          </p>
        )
      } -->
    </div>
    <!-- <ul class="grid mt-8 text-left gap-y-4">
      {
        product.information.map((item) => (
          <li class="flex items-start gap-3 text-gray-800">
            <Tick class="w-6 h-6" />
            <span>{item}</span>
          </li>
        ))
      }
    </ul> -->
    <div class="flex flex-col mt-8">
      {
        product.information.map((item) => (
        <div class="flex flex-row justify-center items-center mt-5">
            <Icon class="w-8 h-8" name="uil:file-download" />
            <Link href={item.link || "#"} block class="hover:underline" style=inverted>
                {item.text}
            </Link>
        </div>
        ))
      }
    </div>
  </div>
</div>
